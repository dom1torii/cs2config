bind space "+jump"
bind mwheeldown "+jump"
bind mwheelup "+jump"
bind ctrl "+duck"
bind shift "+sprint"
bind w "+forward"
bind a "+left"
bind s "+back"
bind d "+right"

option_duck_method "false"
option_speed_method "false"
